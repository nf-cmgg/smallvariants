/*
========================================================================================
    Nextflow config file for HyperCap runs
========================================================================================
*/

params {
    callers             = "vardict"
    scatter_count       = 5
    only_pass           = true
}
process {
    withName: PROCESS_BEDS {
        memory = 64.GB
    }
}
